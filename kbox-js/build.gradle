description = "A utility library for Kotlin for the JS platform"

dependencies {
    testImplementation "ch.tutteli.atrium:atrium-fluent-en_GB-js:$atrium_version", excluding {
        kotlin()
        kbox()
    }
    testImplementation "org.spekframework.spek2:spek-dsl-js:$spek_version"
}
